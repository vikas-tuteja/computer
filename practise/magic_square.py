class MagicSquare(object):
    def __init__(self, N, dct):
        self.size = N
        self.num_of_elems = N*N
        self.dct = dct


    def __validate__():
        if self.dct[0][0] != 1:
            print "First elem is not 1"

        else:
            for 
